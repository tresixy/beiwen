# AI 驱动的无限合成资源网页游戏

一个基于 Phaser 3 的回合制合成游戏，支持 AI 生成合成规则和物品。

## ✨ 特性

- 🎮 回合制合成玩法：抽牌、合成、世界化形、回合结算
- 🤖 AI 驱动（可选）：使用 OpenAI 生成创意合成结果
- 🎴 卡牌系统：集体记忆牌库、稀有度、属性系统
- 🌍 世界沙盘：地块、建筑、单位系统
- 📊 资源管理：食物、生产力、研究点
- 🔖 社会契约：分支选择影响文明路线
- 📱 移动端支持：虚拟摇杆、响应式 HUD
- 🎨 深色游戏化 UI：液态毛玻璃、渐变效果

## 🚀 快速开始

### 方式一：自动安装（推荐）

```bash
# 1. 进入项目目录
cd /root/minigame

# 2. 运行设置脚本（自动安装PostgreSQL、Redis并初始化数据库）
./setup.sh

# 3. 配置 Nginx 反向代理（可选）
./setup-nginx.sh

# 4. 启动游戏服务器
./start.sh
```

**访问地址**:
- 使用 Nginx: http://localhost
- 直接访问: http://localhost:3000

### 方式二：手动安装

#### 环境要求
- Node.js >= 20
- PostgreSQL >= 14
- Redis >= 6

#### 1. 安装依赖
```bash
npm install
npm --prefix client install
```

#### 2. 配置环境变量
```bash
# .env 文件已自动创建，可根据需要修改
# 主要配置项：
# - DATABASE_URL: PostgreSQL 连接字符串
# - REDIS_URL: Redis 连接字符串
# - AI_ENABLED: 是否启用 AI 合成（需要 OPENAI_API_KEY）
```

#### 3. 初始化数据库
```bash
# 确保 PostgreSQL 和 Redis 正在运行
sudo systemctl start postgresql
sudo systemctl start redis-server

# 创建数据库
sudo -u postgres psql -c "CREATE DATABASE minigame;"

# 初始化表
sudo -u postgres psql -d minigame < server/db/schema.sql
```

#### 4. 启动服务器
```bash
# 开发模式（前后端分离运行）
npm run dev           # 后端 API（3000端口）
npm run client:dev    # 前端 React/Vite 开发服务器（默认 8080 端口）

# 生产模式
npm run client:build  # 构建前端静态资源到 client/dist
npm start             # 启动 Express，自动托管 client/dist
```

## 🎮 游戏玩法

### 1. 注册/登录
首次访问需要注册账号，已有账号可直接登录。

### 2. 四阶段回合循环

#### 灵感涌现
点击"抽牌"从集体记忆牌库抽取卡牌到手牌。

#### 引导合成
1. 选择 2 张或多张手牌（点击卡牌选中）
2. 点击"合成"按钮打开思想熔炉
3. 输入合成名称（如"蒸汽引擎"）
4. 点击"合成"生成新物品

#### 现实化形
合成的物品会自动添加到背包，可以在世界中放置建筑或单位。

#### 回合结算
点击"结束回合"进行：
- 资源结算（食物、生产力、研究点）
- 随机事件触发
- 宏伟工程进度推进
- 社会契约选择（每10回合）

### 3. 资源系统
- 🍖 **食物**：维持单位，基础产出 +5/回合
- ⚙️ **生产力**：建造建筑和工程，基础产出 +3/回合
- 🔬 **研究点**：解锁科技和蓝图，基础产出 +2/回合

### 4. 合成系统

#### 规则合成
内置合成规则（优先级最高）：
- 火焰 + 水流 = 蒸汽
- 泥土 + 水流 = 泥浆
- 火焰 + 泥土 = 陶器
- 石头 + 火焰 = 熔炉
- 木材 + 石头 = 斧头
- 金属 + 火焰 = 钢铁
- 能量 + 金属 = 机器

#### AI 合成
启用 AI 后，未命中规则的合成会由 AI 创造性生成：
- 根据输入物品特性合理推理
- 生成物品名称、等级、属性、描述
- 结果缓存 7 天

## 📊 数据库结构

主要表：
- `users` - 用户账号
- `cards` - 卡牌模板
- `deck_cards` - 用户牌库
- `items` - 物品
- `inventories` - 背包
- `resources` - 资源
- `entities` - 世界实体
- `ai_recipes` - AI 合成配方缓存

## 🔧 技术栈

### 前端
- React 19 + Vite
- Phaser 3.60.0（React 容器内运行）
- 单页应用路由 + Token 会话保持
- 液态毛玻璃深色 HUD、移动端虚拟摇杆

### 后端
- Node.js 20 LTS + Express
- PostgreSQL 14（主存储）
- Redis 6（缓存+限流）
- Socket.io（多人预留）

### AI 服务
- OpenAI GPT-4（可选）
- DALL-E 3 图片生成（可选）

## 🛡️ 安全特性

- JWT 认证（7天有效期）
- Argon2 密码哈希
- 登录限流（5次/分钟/IP）
- 合成限流（10次/分钟/用户）
- 图片生成限流（2次/分钟/用户）
- Helmet 安全头
- Zod 输入校验

## 📁 项目架构

### 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                     客户端层 (Client)                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │   React UI   │  │  Phaser 3   │  │  State Hook │  │
│  │  组件系统    │  │  游戏引擎    │  │  状态管理    │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
                         │ HTTP/WebSocket
                         ▼
┌─────────────────────────────────────────────────────────┐
│                     服务端层 (Server)                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  Express API │  │  业务逻辑    │  │  游戏引擎    │  │
│  │  路由层      │  │  Services    │  │  Gameplay   │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
         │              │              │
         ▼              ▼              ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│ PostgreSQL  │  │    Redis     │  │   OpenAI     │
│  主数据库    │  │  缓存/限流   │  │  AI服务      │
└──────────────┘  └──────────────┘  └──────────────┘
```

### 目录结构

```
/root/minigame/
├── client/                      # React + Phaser 前端应用
│   ├── src/
│   │   ├── App.jsx              # 应用根组件（路由/状态）
│   │   ├── main.jsx             # React 入口
│   │   ├── PhaserGame.jsx       # Phaser 容器组件
│   │   ├── components/          # UI 组件
│   │   │   ├── auth/           # 登录/注册
│   │   │   ├── lobby/          # 大厅（六边形地图）
│   │   │   ├── game/           # 游戏内组件
│   │   │   │   ├── HUD.jsx
│   │   │   │   ├── CardsPanel.jsx
│   │   │   │   ├── ForgePanel.jsx
│   │   │   │   ├── InventoryPanel.jsx
│   │   │   │   ├── ProfessionPanel.jsx
│   │   │   │   ├── ContractPanel.jsx
│   │   │   │   └── BottomControls.jsx
│   │   │   └── common/         # 通用组件
│   │   ├── game/                # Phaser 游戏逻辑
│   │   │   ├── MainScene.js    # 主场景（世界网格/单位）
│   │   │   ├── EventBus.js     # React-Phaser 事件桥
│   │   │   └── startGame.js    # Phaser 启动配置
│   │   ├── hooks/               # React Hooks
│   │   │   └── useGameSimulation.js  # 单机游戏状态模拟
│   │   ├── data/                # 静态数据定义
│   │   │   ├── cards.js        # 卡牌生成逻辑
│   │   │   ├── professions.js  # 职业数据
│   │   │   ├── contracts.js    # 契约模板
│   │   │   └── inventory.js    # 背包物品
│   │   ├── services/            # API 服务封装
│   │   │   └── api.js          # HTTP 请求封装
│   │   └── styles/             # 全局样式
│   │       └── app.css         # 液态毛玻璃主题
│   ├── public/                 # 静态资源
│   │   ├── assets/2d/          # 2D 美术资源
│   │   └── favicon.png
│   ├── vite/                   # Vite 配置
│   └── dist/                   # 构建输出（生产）
│
├── server/                      # Express 后端服务
│   ├── index.js                # 服务器入口
│   ├── config/                 # 配置模块
│   │   └── env.js              # 环境变量管理
│   ├── db/                     # 数据库层
│   │   ├── connection.js       # PostgreSQL 连接池
│   │   ├── redis.js            # Redis 客户端
│   │   ├── schema.sql          # 数据库表结构
│   │   └── create-admin.js     # 管理员创建脚本
│   ├── routes/                 # API 路由层
│   │   ├── auth.js             # 认证路由
│   │   ├── inventory.js        # 背包路由
│   │   ├── synthesize.js       # 合成路由
│   │   ├── deck.js             # 牌库路由
│   │   ├── turn.js             # 回合路由
│   │   ├── world.js            # 世界路由
│   │   ├── contract.js         # 契约路由
│   │   ├── blueprint.js        # 蓝图路由
│   │   ├── image.js            # 图片生成路由
│   │   ├── profession.js       # 职业路由
│   │   └── admin.js            # 管理路由
│   ├── services/               # 业务逻辑层
│   │   ├── authService.js      # 认证服务
│   │   ├── inventoryService.js # 背包服务
│   │   ├── synthService.js     # 合成规则引擎
│   │   ├── aiService.js        # AI 合成服务
│   │   ├── imageService.js     # 图片生成服务
│   │   └── professionService.js # 职业服务
│   ├── gameplay/               # 游戏逻辑层
│   │   ├── deckService.js      # 抽牌/洗牌
│   │   ├── blueprintService.js # 蓝图生成
│   │   ├── worldService.js     # 世界沙盘
│   │   ├── contractService.js   # 社会契约
│   │   ├── resourceService.js  # 资源计算
│   │   └── turnService.js      # 回合推进
│   ├── rules/                  # 规则定义
│   │   └── customRules.js      # 自定义合成规则
│   ├── sockets/                # WebSocket（预留）
│   │   └── index.js            # Socket.io 初始化
│   └── utils/                  # 工具函数
│       ├── validators.js       # Zod 校验
│       ├── logger.js           # Pino 日志
│       └── security.js         # 安全中间件（JWT/限流）
│
├── package.json                # 根依赖配置
├── .env                        # 环境变量
├── setup.sh                   # 一键安装脚本
├── start.sh                   # 开发启动脚本
├── start-production.sh         # 生产启动脚本
├── check-server.sh             # 服务健康检查
├── nginx.conf                  # Nginx 配置模板
└── README.md                   # 项目文档
```

### 数据流

#### 认证流程
```
用户输入 → AuthScreen → api.js → /api/auth/login → authService → PostgreSQL
   ↓
Token + User → localStorage → App.jsx → GameShell/Lobby
```

#### 游戏流程（单机模拟）
```
用户操作 → React组件 → useGameSimulation Hook → 本地状态更新
   ↓
合成完成 → forgeResultToInventoryItem → inventory 状态 → InventoryPanel 展示
   ↓
回合结束 → endTurn → 资源更新 + 契约生成 → HUD 刷新
```

#### Phaser 交互
```
React 事件 → EventBus.emit('forge:flash') → MainScene.handleForgeFlash
   ↓
Phaser 场景更新 → 脉冲动画 → 视觉反馈
```

### 状态管理

- **前端**：React Context + Hooks（`useGameSimulation` 管理游戏状态）
- **后端**：PostgreSQL（持久化）+ Redis（缓存/限流）
- **会话**：JWT Token（7天有效期，存储在 localStorage）

### 关键设计模式

1. **前后端分离**：React SPA + Express REST API
2. **组件化**：UI 拆分为独立组件，职责清晰
3. **Hooks 抽象**：游戏逻辑封装在 `useGameSimulation`，便于测试和复用
4. **事件驱动**：React ↔ Phaser 通过 EventBus 通信
5. **数据驱动**：静态数据（cards/professions/contracts）集中管理

## 🔌 API 端点

### 认证
- `POST /api/auth/register` - 注册
- `POST /api/auth/login` - 登录
- `GET /api/auth/me` - 获取当前用户

### 游戏
- `GET /api/deck/draw?count=3` - 抽牌
- `POST /api/synthesize` - 合成物品
- `POST /api/turn/end` - 结束回合
- `GET /api/turn/resources` - 获取资源
- `GET /api/world/state` - 获取世界状态
- `POST /api/contract/choose` - 选择契约

### 背包
- `GET /api/inventory` - 获取背包
- `POST /api/inventory/add` - 添加物品
- `POST /api/inventory/remove` - 移除物品

## 🎯 开发路线图

### M0（已完成）
- ✅ 认证系统
- ✅ 四阶段回合循环
- ✅ 卡牌抽取
- ✅ 合成系统（规则+AI）
- ✅ 资源管理
- ✅ 世界沙盘
- ✅ 社会契约
- ✅ 移动端适配

### M1（计划中）
- 蓝图系统完善
- 事件系统扩展
- 日志和缓存优化
- 图片生成集成

### M2（计划中）
- 多人实时对战
- PWA 离线支持
- CDN 资源优化
- 宏伟工程可视化

## 🐛 故障排查

### 端口被占用
```bash
# 修改 .env 中的 PORT 配置
PORT=3001
```

### 数据库连接失败
```bash
# 检查 PostgreSQL 状态
sudo systemctl status postgresql

# 重启 PostgreSQL
sudo systemctl restart postgresql
```

### Redis 连接失败
```bash
# 检查 Redis 状态
sudo systemctl status redis-server

# 重启 Redis
sudo systemctl restart redis-server
```

## 📝 许可证

MIT

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

